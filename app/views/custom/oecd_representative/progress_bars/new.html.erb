<% provide :title do %>
  <%= "#{t("oecd_representative.header.title")} - #{t("oecd_representative.progress_bars.new.creating")}" %>
<% end %>

<%= back_link_to progress_bars_index %>

<h2><%= t("oecd_representative.progress_bars.new.creating") %></h2>

<%= render "form" %>
