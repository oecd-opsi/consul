<% provide :title do %>
  <%= t("oecd_representative.header.title") %> - <%= t("admin.menu.legislation") %> - <%= @process.title %> - <%= t("admin.legislation.proposals.index.title") %>
<% end %>

<div class="legislation-admin legislation-draft-versions-index">

  <%= back_link_to oecd_representative_legislation_processes_path, t("admin.legislation.proposals.index.back") %>

  <h2><%= @process.title %></h2>

  <%= render "oecd_representative/legislation/processes/subnav", process: @process, active: "proposals" %>

  <%= render "form" %>
  <%= render "proposals", proposals: @proposals %>
</div>
