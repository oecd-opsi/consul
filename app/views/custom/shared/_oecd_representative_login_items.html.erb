<% if show_admin_menu?(current_user) %>
  <li class="has-submenu">
    <%= link_to t("layouts.header.administration_menu"), "#", rel: "nofollow", class: "hide-for-small-only" %>
    <ul class="submenu menu vertical" data-submenu>
      <% if current_user.oecd_representative? %>
        <li>
          <%= link_to t("layouts.header.oecd_representative"), oecd_representative_root_path %>
        </li>
      <% end %>
    </ul>
  </li>
<% end %>
