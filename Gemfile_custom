gem 'auth0'
gem 'aws-sdk-rails'
gem 'omniauth-auth0', '~> 2.2'
gem 'enumerize'

group :development, :test do
  gem 'dotenv-rails'
  gem 'pry-rails', '~> 0.3.4'
end

group :development do
  gem "lefthook"
end

group :production, :staging do
  gem "sentry-raven"
end
